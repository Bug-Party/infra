After [installing gcloud cli](https://cloud.google.com/sdk/docs/install), 
run these commands:
```
gcloud auth login
gcloud auth application-default login
gcloud components install gke-gcloud-auth-plugin
```
